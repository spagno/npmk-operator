apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: npmk-operator-system

resources:
- ../crd
- ../rbac
- ../manager

images:
- name: controller
  newName: npmk-operator
  newTag: latest
