apiVersion: v1
kind: Service
metadata:
  name: npmk-operator-metrics
  labels:
    app: npmk-operator
spec:
  ports:
  - name: metrics
    port: 8080
    targetPort: 8080
    protocol: TCP
  selector:
    app: npmk-operator
